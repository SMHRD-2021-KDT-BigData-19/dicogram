<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM "mybatis-3-mapper.dtd" >
<mapper namespace="com.dicogram.database.MemberMapper">
	<select id="test" parameterType="com.dicogram.domain.dUsers"
		resultType="com.dicogram.domain.dUsers">
		select * from dUsers where id = #{id} and pw = #{pw}
	</select>
</mapper>
